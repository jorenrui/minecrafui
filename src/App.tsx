import { Game } from '@components/Canvas';
import { ColorPicker } from '@components/ColorPicker';

function App() {
  return (
    <main>
      <ColorPicker />
      <Game />
    </main>
  )
}

export default App
